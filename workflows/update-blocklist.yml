# name: update-blocklist

# permissions: {}

# on:
#   schedule:
#     - cron: "0 4 * * 0"
#   workflow_dispatch:

# concurrency:
#   group: ${{ github.workflow }}
#   cancel-in-progress: false

# jobs:
#   update-blocklist:
#     name: Download and update block list
#     runs-on: ubuntu-24.04
#     permissions:
#       contents: write # required to commit updated blocklist.txt
#       pull-requests: write # required to open/update the pull request
#     steps:
#       - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
#         with:
#           persist-credentials: false

#       - uses: jdx/mise-action@c1ecc8f748cd28cdeabf76dab3cccde4ce692fe4 # v4.0.0
#         with:
#           install: true
#           cache: true
#           install_args: uv

#       - run: uv sync

#       - name: Download and save block lists
#         run: uv run python scripts/build_blocklist.py

#       - name: Open pull request
#         uses: peter-evans/create-pull-request@c0f553fe549906ede9cf27b5156039d195d2ece0 # v8.1.0
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           add-paths: blocklist/
#           commit-message: "chore: update community block lists"
#           branch: update-blocklist
#           delete-branch: true
#           title: "chore: update community block lists"
#           body: |
#             Automated weekly refresh of per-source `blocklist/*.txt` files from upstream sources.
#           labels: blocklist
